# List of the premium features in EE edition.
#
# NOTE (Mobílli): intentionally EMPTY. This self-hosted fork unlocks all premium
# features, so the nightly Internal::ReconcilePlanConfigService must not disable
# any feature on the community plan. Features are still gated by the normal
# per-account feature flags (enable them in Super Admin → Accounts → Features).
# The matching frontend paywalls are also disabled via PREMIUM_FEATURES in
# app/javascript/dashboard/featureFlags.js.
[]
